<?xml version="1.0"?>
<odoo>
    <record id="res_partner_view_form" model="ir.ui.view">
        <field name="name">res.partner.view.form.inherit.rental_vehicle</field>
        <field name="model">res.partner</field>
        <field name="inherit_id" ref="base.view_partner_form"/>
        <field name="arch" type="xml">
            <xpath expr="//page[@name='internal_notes']" position="before">
                <page name='paper_ids' string="Documents">
                    <field name="paper_ids" >
                        <tree editable="top" >
                            <field name="type" />
                            <field name="delivery_date" />
                            <field name="delivery_place" />
                            <!-- <field name="file" /> -->
                            <field name="attachment_ids" widget="many2many_binary" string="Document" class="g-col-8"/>
                        </tree>
                    </field>
                </page>
            </xpath>
        </field>
    </record>
</odoo>
